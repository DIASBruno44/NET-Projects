<Window x:Class="MeteoApp.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:MeteoApp.WPF.Converters"
        xmlns:vm="clr-namespace:MeteoApp.ViewModels;assembly=MeteoApp.ViewModels"
        
        mc:Ignorable="d"
        Title="Météo Client WPF" Height="450" Width="400"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:MeteoIconConverter x:Key="MeteoIconConverter"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,15">
            <TextBox Text="{Binding NomVille, UpdateSourceTrigger=PropertyChanged}" Width="200" Padding="5" FontSize="14" />
            <Button Content="Rechercher" Command="{Binding RechercherMeteoCommand}" Margin="10,0,0,0" Padding="10,0"/>
        </StackPanel>

        <Border Grid.Row="1" Padding="20" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5"
            Visibility="{Binding HasNoMeteoData, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       FontWeight="SemiBold" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"/>
                <TextBlock Text="Bienvenue ! 🌍" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="Entrez le nom d'une ville pour obtenir les prévisions." Foreground="#666"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="0,15,0,0"
            Visibility="{Binding HasMeteoData, Converter={StaticResource BooleanToVisibilityConverter}}">

            <TextBlock Text="{Binding MeteoDuJour.NomVille}" FontSize="32" FontWeight="Bold" Foreground="#333"/>

            <Image Height="100" Width="100" HorizontalAlignment="Left" Margin="0,0,0,10"
           Source="{Binding MeteoDuJour.Descriptions[0].IconeCode, Converter={StaticResource MeteoIconConverter}}"/>

            <TextBlock FontSize="72" FontWeight="Light" Margin="0,10,0,10" Foreground="#007ACC">
        <Run Text="{Binding MeteoDuJour.DonnéesPrincipales.TemperatureActuelle, StringFormat=\{0:F1\}}"/>
        <Run Text="°C"/>
            </TextBlock>

            <TextBlock Text="{Binding MeteoDuJour.Descriptions[0].Description}" FontSize="20" FontStyle="Italic" Foreground="#666"/>

            <Separator Margin="0,10"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock FontWeight="SemiBold" Text="Vent : "/>
                <TextBlock Text="{Binding MeteoDuJour.Vent.Vitesse, StringFormat=\{0:F2\} m/s}"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock FontWeight="SemiBold" Text="Humidité : "/>
                <TextBlock Text="{Binding MeteoDuJour.DonnéesPrincipales.Humidite, StringFormat=\{0\}%}"/>
            </StackPanel>



        </StackPanel>

    </Grid>
</Window>
